# Backend
DATABASE_URL="postgresql://rizza:rizza@localhost:5432/rizza_booking?schema=public"
PORT=3000
CORS_ORIGIN="http://localhost:4200"

# SMTP (optional - if empty, backend will skip sending email)
SMTP_HOST=""
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="RIZZA <no-reply@example.com>"
MAIL_BCC_INTERNAL=""
